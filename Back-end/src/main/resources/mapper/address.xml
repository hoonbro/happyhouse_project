<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.AddressDao">
	<select id="getSido" resultType="String">
		select sido_name from sidocode;
	</select>

	<select id="searchGugun" parameterType="String" resultType="Address">
		select distinct gugun from baseaddress where city = #{sido}
	</select>
	
	<select id="searchDong" parameterType="String" resultType="Address">
		select distinct dong, lat, lng from baseaddress where gugun = #{gugun}
	</select>
	
	<select id="centerMark" parameterType="map" resultType="Address">
		select lat, lng
		from baseaddress
		where city = #{sido} and gugun = #{gugun} and dong = #{dong}
	</select>
</mapper>