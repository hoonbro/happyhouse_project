<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.BoardDao">

	<select id="boardList" resultType="board">
		select num, userId, password, title, content, date
		from board
	</select>
	
	<select id="searchList" parameterType="String" resultType="board">
		select num, userId, password, title, content, date
		from board
		where userId = #{userId};
	</select>
	
	<insert id = "writeBoard" parameterType="board">
		insert into board (userId, password, title, content, date)
		values(#{userId}, #{password}, #{title}, #{content}, now())
	</insert>
	
	<delete id="deleteBoard" parameterType="int">
		delete from board
		where num = #{num}
	</delete>
	
	<update id="modifyBoard" parameterType="Board">
		update board set title = #{title}, content = #{content}
		where num = #{num} 
	</update>
	<select id="searchPost" parameterType="String" resultType="board">
		select num, userId, password, title, content, date
		from board
		where title like CONCAT('%', #{searchInput}, '%')

	</select>
</mapper>

