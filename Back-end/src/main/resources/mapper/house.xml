<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.HouseDao">
	<select id="findDong" parameterType="String" resultType="house">
		select AptName, dealAmount, area, buildYear
		from housedeal 
		where dong = #{dong}
	</select>
	
	<select id="findApt" parameterType="String" resultType="house">
	select AptName, dealAmount, area, buildYear 
	from housedeal 
	where AptName like #{aptinput}
	</select>
	
	<select id="houseList" resultType="house">
		select AptName, lat, lng 
		from houseinfo
	</select>
</mapper>